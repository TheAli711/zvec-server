## Summary

<!-- What does this PR change and why? -->

## Ticket

<!-- e.g. Closes: ZS-042 (spec SPEC-011). Every behavior change needs a ticket;
     see specs/README.md. -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation
- [ ] Refactor / internal change
- [ ] CI / build / tooling

## Checklist

- [ ] `uv run ruff check` passes
- [ ] `uv run ruff format --check` passes
- [ ] `uv run mypy` passes
- [ ] `uv run pytest` passes (new tests added for new behavior)
- [ ] Docs updated (`README.md`, `docs/`, `examples/`) where relevant
- [ ] `CHANGELOG.md` updated under the `Unreleased` section
- [ ] Ticket flipped to `done` and `TRACKER.md` regenerated (`python scripts/tracker.py`)
- [ ] Change respects the "thin layer over Zvec" scope

## Notes for reviewers

<!-- Anything reviewers should pay special attention to. -->
